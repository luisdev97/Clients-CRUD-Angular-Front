import { Region } from './Region';

class Client {
    id: number;
    name: string;
    surname: string;
    createAt: string;
    email: string;
    img: string;
    region: Region;
}

export default Client;
